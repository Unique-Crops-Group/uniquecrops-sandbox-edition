package com.remag.uniquecrops.blocks.tiles;

import com.remag.uniquecrops.init.UCTiles;
import net.minecraft.core.BlockPos;
import net.minecraft.world.level.block.state.BlockState;

public class TileSucco extends BaseTileUC {

    public TileSucco(BlockPos pos, BlockState state) {

        super(UCTiles.SUCCO.get(), pos, state);
    }
}
